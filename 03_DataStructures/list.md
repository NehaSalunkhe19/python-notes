# List in Python

A **list** is an ordered and mutable data structure in Python.

## Key Points
- Ordered
- Mutable
- Allows duplicate values

## Example
```python
numbers = [1, 2, 3, 4]
numbers.append(5)
print(numbers)
